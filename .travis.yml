language: go
go:
  - 1.13.x
  - 1.14.x
  - 1.15.x
  - 1.x
  - master

install:
  - go get -u ./cmd/wmidx
  - docker-compose up -d

script:
  - go build -o bin/wmidx ./cmd/wmidx # test for error-free build
  - go test ./... # unit tests